name: opengrow_flutterflow_messaging
description: FlutterFlow client for canonical OpenGrow Support, with Messaging compatibility aliases.
version: 1.3.0
publish_to: none

environment:
  sdk: ^3.9.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ">=1.2.0 <2.0.0"
  web_socket_channel: ">=3.0.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
