{
  "name": "@superboard/products-worker",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.runtime.json",
    "test": "vitest run && INTERNAL_API_TOKEN=products-runtime-secret vitest run --config vitest.runtime.config.ts",
    "test:unit": "vitest run",
    "test:runtime": "INTERNAL_API_TOKEN=products-runtime-secret vitest run --config vitest.runtime.config.ts"
  },
  "dependencies": {
    "@superboard/contracts": "*",
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.21.3",
    "@cloudflare/workers-types": "^5.20260815.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  }
}
